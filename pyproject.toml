[project]
name = "cloudcoil.models.sealed_secrets"
version = "0.0.0"
description = "Versioned sealed-secrets models for cloudcoil"
authors = [{ name = "Sambhav Kothari", email = "sambhavs.email@gmail.com" }]
maintainers = [{ name = "Sambhav Kothari", email = "sambhavs.email@gmail.com" }]
license = { text = "Apache-2.0" }
classifiers = [
    "Programming Language :: Python :: 3.14",
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "cloudcoil>=0.7.1,<0.8",
]
keywords = ["cloud-native", "kubernetes", "sealed-secrets", "pydantic", "python", "async", "cloudcoil", "cloudcoil-models"]

[project.urls]
Homepage = "https://github.com/cloudcoil/cloudcoil"
Documentation = "https://cloudcoil.github.io/cloudcoil"
Repository = "https://github.com/cloudcoil/models-sealed-secrets"
Issues = "https://github.com/cloudcoil/models-sealed-secrets/issues"
Changelog = "https://github.com/cloudcoil/models-sealed-secrets/releases"

[build-system]
requires = ["hatchling>=1.18.0"]
build-backend = "hatchling.build"

[tool.ruff]
line-length = 100
target-version = "py314"

[tool.ruff.lint]
select = [
    "E", # pycodestyle
    "F", # pyflakes
    "B", # flake8-bugbear
    "N", # pep8-naming
    "I", # isort
]
ignore = ["E203", "B008", "N818", "E501", "B904"]

[tool.pytest.ini_options]
addopts = "--import-mode=importlib -ra -q --cov=cloudcoil --cov-report=xml --cov-report=term -vvv --junitxml=junit.xml -o junit_family=legacy"
testpaths = ["tests"]
asyncio_mode = "auto"

[dependency-groups]
dev = [
    "mypy>=1.19",
    "pyright>=1.1.407",
    "mkdocs>=1.6.1",
    "mkdocs-material>=9.5.3",
    "mkdocstrings>=0.24.0",
    "mkdocstrings-python>=1.7.5",
    "mkdocs-autorefs>=0.5.0",
    "pytest>=8.3.4",
    "pytest-cov>=4.1.0",
    "ruff>=0.14",
    "types-pyyaml>=6.0.12.20241221",
    "ipython>=8.12.3",
    "pytest-sugar>=1.0.0",
    "pytest-asyncio>=0.25.0",
    "cloudcoil[codegen,test]>=0.7.1,<0.8"
]

[tool.mypy]
python_version = "3.14"

[tool.coverage.run]
omit = ["cloudcoil/models/**"]

[tool.hatch.build.targets.wheel]
only-include = ["cloudcoil"]

[[tool.cloudcoil.codegen.models]]
# Python package for generated models
namespace = "cloudcoil.models.sealed_secrets"
input = [
    "https://raw.githubusercontent.com/bitnami/sealed-secrets/refs/tags/v0.39.1/helm/sealed-secrets/crds/bitnami.com_sealedsecrets.yaml",
]
